{%- capture subject -%}
Comment: {{ page.title }}
{%- endcapture -%}

{%- capture body -%}
post: {{ page.id }}
{%- if include.repliesTo != "" %}
repliesTo: {{ include.repliesTo }}
{%- endif %}
# The name will be linked to your email address.
# If you change your name, your old name will be replaced in all comments.
name:
# The password is used to authenticate against your email address.
# The password is linked to your email address after your first comment.
# After the first comment, you have to keep using the same password.
# The password is sent in plaintext over email.
# If you want extra security, you can encrypt this email using the public key
# found at {{ site.url }}/{{ site.pgp }}
password:
comment: |-
  # The comment can be markdown text.
  # Write your comment under this line. Align it with the '#' character.
{%- endcapture -%}

{%- capture href -%}
mailto:{{ site.mailto }}?subject={{ subject }}&body={{ body }}
{%- endcapture -%}

href={{ href | uri_escape | replace: "#", "%23" }}
